import React from 'react';

const Footer = (props) => {
    return (
        <footer class="footer">
            Copyright &copy; {new Date().getFullYear()}
        </footer>
    );
};

export default Footer;