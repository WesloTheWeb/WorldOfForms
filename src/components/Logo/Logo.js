import React from 'react';
import { globe } from '../../assets/assets';

const Logo = (props) => {
    return (
        <figure>
            <img src={globe} alt="outline of a globe" />
        </figure>
    );
};

export default Logo;